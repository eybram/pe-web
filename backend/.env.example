# Example .env for backend DB connection
# Copy this to `.env` and fill with your database credentials.

# Use 'sqlsrv' for Microsoft SQL Server, 'mysql' for MySQL
DB_DRIVER=sqlsrv
DB_HOST=127.0.0.1
DB_PORT=1433
DB_NAME=BrokenPocket
DB_USER=sa
DB_PASS=YourStrongPassword

# Optional: if you want to run PHP built-in server you can set it here
# SERVER_HOST=127.0.0.1
# SERVER_PORT=8000
